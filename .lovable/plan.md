

# Hide Lovable Badge

Add the following CSS rule at the end of `src/index.css`:

```css
#lovable-badge {
  display: none !important;
}
```

This will globally hide any element with the ID `lovable-badge` across the entire app.

